name: tasks_api
description: A new Dart Frog application
version: 1.0.0+1
publish_to: none

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  dart_frog: ^1.1.0
  postgres: ^2.4.0
  dart_frog_cors: ^2.0.0
  mime: ^2.0.0         # ← untuk lookupMimeType, MimeMultipartTransformer
  http_parser: ^4.0.0 
  dart_jsonwebtoken: ^2.0.1
  bcrypt: ^1.1.3
  crypto: ^3.0.0


dev_dependencies:
  mocktail: ^1.0.3
  test: ^1.25.5
  very_good_analysis: ^5.1.0
